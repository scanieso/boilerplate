import ns from 'App';
// import $ from 'jQuery';

ns.UI = {
  initialize() {
    console.log('Initialize: ui.js');

    // $('a.skip-link').on('click', function(e) {
    //   e.preventDefault();
    //   let href = $(this).attr('href'),
    //   $el = $(href);

    //   if (!$el.is(':tabbable')) {
    //     $el.attr('tabindex', '-1');
    //   }
    //   $el.focus();
    // });
  }
};
